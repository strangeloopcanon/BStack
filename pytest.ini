[pytest]
addopts = -q
norecursedirs = third_party .venv build dist
markers =
    bench: planner benchmarks (CPU-only, must finish in under 2 minutes)
filterwarnings =
    ignore:A NumPy version .* is required for this version of SciPy.*:UserWarning
